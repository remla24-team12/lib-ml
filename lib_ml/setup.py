# setup.py

from setuptools import setup, find_packages

setup(
    name='lib-ml',
    version='0.1.0',  # Use a versioning scheme
    packages=find_packages(),
    install_requires=[
        #TODO: add required packages here
    ],
)
